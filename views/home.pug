extends layout/main-layout.pug

block content
    +createMockForm(userLoggedIn)
    .mocksContainer

    +createReplyModal(userLoggedIn)


block scripts
    script(defer="", src="/js/home.js?v=v0.1.0", type="module")
